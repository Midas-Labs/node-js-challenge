<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SnapGrid</title>
  <link rel="stylesheet" href="/styles/main.css"> <!-- Link to your CSS file -->
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
      padding: 0;
      background: linear-gradient(135deg, #f7f7f7, #e2e2e2);
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }
    .main-container {
      display: flex;
      justify-content: space-between;
      align-items: center;
      max-width: 1200px;
      width: 100%;
      padding: 2rem;
    }
    .text-container, .logo-container {
      flex: 1;
      margin: 1rem;
    }
    .text-container {
      text-align: center;
      background: #ffffff;
      padding: 2rem 3rem;
      border-radius: 10px;
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
    }
    .logo-container {
      display: flex;
      justify-content: center;
      align-items: center;
      background: #ffffff;
      padding: 2rem;
      border-radius: 10px;
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
    }
    .logo-container img {
      max-width: 40%;
      height: auto;
      border-radius: 10px;
    }
    h1 {
      margin-bottom: 1rem;
      color: #333;
      font-size: 2.5rem;
      font-weight: 600;
    }
    p {
      margin-bottom: 2rem;
      color: #555;
      font-size: 1.1rem;
    }
    .button {
      display: inline-block;
      margin: 0.5rem;
      padding: 0.75rem 2rem;
      border: none;
      border-radius: 50px;
      color: #fff;
      font-size: 1.1rem;
      text-decoration: none;
      cursor: pointer;
      transition: background-color 0.3s, transform 0.3s;
    }
    .button.sign-in {
      background-color: #007bff;
    }
    .button.sign-up {
      background-color: #28a745;
    }
    .button:hover {
      opacity: 0.9;
      transform: translateY(-2px);
    }
    .button:active {
      transform: translateY(0);
    }
  </style>
</head>
<body>
    <div class="main-container">
      <div class="text-container">
        <h1>Welcome to SnapGrid!</h1>
        <p>Your very own stunning visual gallery to share and connect. Join our community today!</p>
        <% if (user) { %>
          <p>Welcome, <%= user.name %>!</p>
          <a href="/profile" class="button sign-in">Profile</a>
          <a href="/logout" class="button sign-in">Log Out</a>
        <% } else { %>
          <a href="/login" class="button sign-in">Sign In</a>
          <a href="/login?screen_hint=signup" class="button sign-up">Sign Up</a>
        <% } %>
      </div>
      <div class="logo-container">
        <img src="https://snapgrid-user-images.s3.us-east-2.amazonaws.com/auth0%7C66e75d8b49202246642e1f00/SnapgridLogo.png" alt="Application Logo">
      </div>
    </div>
  </body>
</html>
